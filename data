<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Flowtrack | Crypto Derivatives Intelligence</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chartjs-adapter-date-fns"></script>
    <style>
        :root {
            --bg: #0a0e17;
            --bg-secondary: #0d1321;
            --panel: #111827;
            --panel-hover: #1a2332;
            --border: #1e2a3b;
            --border-light: #2d3a4f;
            --text: #e5e7eb;
            --text-muted: #9ca3af;
            --text-dim: #6b7280;
            --accent: #3b82f6;
            --accent-glow: rgba(59, 130, 246, 0.2);
            --good: #10b981;
            --good-dim: #059669;
            --good-bg: rgba(16, 185, 129, 0.12);
            --warn: #f59e0b;
            --warn-bg: rgba(245, 158, 11, 0.12);
            --bad: #ef4444;
            --bad-bg: rgba(239, 68, 68, 0.12);
            --purple: #8b5cf6;
            --cyan: #06b6d4;
            --orange: #f97316;
            --gradient: linear-gradient(135deg, #06b6d4 0%, #3b82f6 50%, #8b5cf6 100%);
        }
        
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: var(--bg);
            color: var(--text);
            min-height: 100vh;
            line-height: 1.5;
        }
        
        /* Navigation */
        .nav {
            background: rgba(13, 19, 33, 0.95);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid var(--border);
            padding: 14px 24px;
            position: sticky;
            top: 0;
            z-index: 100;
        }
        
        .nav-content {
            max-width: 1800px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 20px;
            flex-wrap: wrap;
        }
        
        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
        }
        
        .logo-icon {
            width: 38px;
            height: 38px;
            background: var(--gradient);
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 900;
            font-size: 16px;
            color: white;
            box-shadow: 0 4px 20px var(--accent-glow);
        }
        
        .logo-text {
            font-size: 22px;
            font-weight: 900;
            background: var(--gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .nav-tabs {
            display: flex;
            gap: 4px;
            background: var(--bg-secondary);
            padding: 4px;
            border-radius: 10px;
            border: 1px solid var(--border);
        }
        
        .nav-tab {
            padding: 8px 16px;
            border-radius: 8px;
            font-size: 13px;
            font-weight: 600;
            color: var(--text-muted);
            cursor: pointer;
            transition: all 0.2s;
            border: none;
            background: none;
        }
        
        .nav-tab:hover { color: var(--text); }
        .nav-tab.active { background: var(--panel); color: var(--text); box-shadow: 0 2px 8px rgba(0,0,0,0.2); }
        
        .nav-status {
            display: flex;
            align-items: center;
            gap: 16px;
        }
        
        .status-pill {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 6px 14px;
            background: var(--panel);
            border: 1px solid var(--border);
            border-radius: 100px;
            font-size: 12px;
            font-weight: 600;
        }
        
        .status-dot {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background: var(--good);
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }
        
        .refresh-btn {
            background: var(--panel);
            border: 1px solid var(--border);
            color: var(--text);
            padding: 8px 14px;
            border-radius: 8px;
            cursor: pointer;
            font-size: 12px;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 6px;
            transition: all 0.2s;
        }
        
        .refresh-btn:hover { background: var(--panel-hover); border-color: var(--border-light); }
        .refresh-btn:disabled { opacity: 0.5; cursor: not-allowed; }
        
        /* Container */
        .container {
            max-width: 1800px;
            margin: 0 auto;
            padding: 20px;
        }
        
        /* API Key Banner */
        .api-banner {
            background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(139, 92, 246, 0.1));
            border: 1px solid var(--accent);
            border-radius: 12px;
            padding: 16px 20px;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 16px;
        }
        
        .api-banner-icon {
            font-size: 24px;
        }
        
        .api-banner-text {
            flex: 1;
        }
        
        .api-banner-title {
            font-size: 14px;
            font-weight: 700;
            margin-bottom: 4px;
        }
        
        .api-banner-desc {
            font-size: 12px;
            color: var(--text-muted);
        }
        
        .api-input-group {
            display: flex;
            gap: 8px;
        }
        
        .api-input {
            background: var(--bg-secondary);
            border: 1px solid var(--border);
            color: var(--text);
            padding: 8px 12px;
            border-radius: 6px;
            font-size: 12px;
            width: 280px;
            font-family: monospace;
        }
        
        .api-btn {
            background: var(--accent);
            border: none;
            color: white;
            padding: 8px 16px;
            border-radius: 6px;
            font-size: 12px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s;
        }
        
        .api-btn:hover { opacity: 0.9; }
        
        /* Grid */
        .grid-2 { display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px; }
        .grid-3 { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; }
        .grid-4 { display: grid; grid-template-columns: repeat(4, 1fr); gap: 16px; }
        
        @media (max-width: 1200px) {
            .grid-4 { grid-template-columns: repeat(2, 1fr); }
        }
        @media (max-width: 800px) {
            .grid-2, .grid-3, .grid-4 { grid-template-columns: 1fr; }
        }
        
        /* Cards */
        .card {
            background: var(--panel);
            border: 1px solid var(--border);
            border-radius: 14px;
            padding: 18px;
            transition: all 0.2s;
        }
        
        .card:hover {
            border-color: var(--border-light);
        }
        
        .card-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 12px;
        }
        
        .card-title {
            font-size: 12px;
            font-weight: 600;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .card-badge {
            font-size: 10px;
            font-weight: 700;
            padding: 3px 8px;
            border-radius: 100px;
            text-transform: uppercase;
            letter-spacing: 0.3px;
        }
        
        .badge-good { background: var(--good-bg); color: var(--good); }
        .badge-warn { background: var(--warn-bg); color: var(--warn); }
        .badge-bad { background: var(--bad-bg); color: var(--bad); }
        .badge-neutral { background: rgba(107, 114, 128, 0.2); color: var(--text-muted); }
        
        .card-value {
            font-size: 28px;
            font-weight: 800;
            margin-bottom: 4px;
            letter-spacing: -0.5px;
        }
        
        .card-value.lg { font-size: 36px; }
        .card-value.good { color: var(--good); }
        .card-value.warn { color: var(--warn); }
        .card-value.bad { color: var(--bad); }
        .card-value.neutral { color: var(--text-muted); }
        
        .card-subtitle {
            font-size: 12px;
            color: var(--text-muted);
        }
        
        .card-change {
            font-size: 12px;
            font-weight: 600;
            margin-top: 6px;
        }
        
        .card-change.positive { color: var(--good); }
        .card-change.negative { color: var(--bad); }
        
        /* Summary Row */
        .summary-row {
            display: grid;
            grid-template-columns: repeat(5, 1fr);
            gap: 14px;
            margin-bottom: 20px;
        }
        
        @media (max-width: 1200px) {
            .summary-row { grid-template-columns: repeat(3, 1fr); }
        }
        @media (max-width: 700px) {
            .summary-row { grid-template-columns: repeat(2, 1fr); }
        }
        
        .summary-card {
            background: var(--panel);
            border: 1px solid var(--border);
            border-radius: 12px;
            padding: 16px;
        }
        
        .summary-label {
            font-size: 11px;
            font-weight: 600;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 8px;
        }
        
        .summary-value {
            font-size: 24px;
            font-weight: 800;
        }
        
        .summary-change {
            font-size: 11px;
            font-weight: 600;
            margin-top: 4px;
        }
        
        /* Chart Container */
        .chart-container {
            position: relative;
            height: 300px;
        }
        
        .chart-container.tall {
            height: 400px;
        }
        
        /* Section */
        .section {
            margin-bottom: 20px;
        }
        
        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 14px;
        }
        
        .section-title {
            font-size: 15px;
            font-weight: 700;
            color: var(--text);
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        /* OI Dominance Bars */
        .oi-dominance-bar {
            height: 32px;
            border-radius: 8px;
            overflow: hidden;
            display: flex;
            margin: 16px 0;
        }
        
        .oi-segment {
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 11px;
            font-weight: 700;
            color: white;
            transition: all 0.3s;
        }
        
        .oi-segment.btc { background: var(--accent); }
        .oi-segment.eth { background: var(--cyan); }
        .oi-segment.others { background: var(--orange); }
        
        .oi-legend {
            display: flex;
            gap: 20px;
            justify-content: center;
            margin-top: 12px;
        }
        
        .oi-legend-item {
            display: flex;
            align-items: center;
            gap: 6px;
            font-size: 12px;
        }
        
        .oi-legend-dot {
            width: 10px;
            height: 10px;
            border-radius: 3px;
        }
        
        /* Funding Bar */
        .funding-meter {
            height: 24px;
            background: linear-gradient(90deg, var(--good) 0%, var(--bg-secondary) 50%, var(--bad) 100%);
            border-radius: 12px;
            position: relative;
            margin: 16px 0;
        }
        
        .funding-pointer {
            position: absolute;
            top: -4px;
            width: 4px;
            height: 32px;
            background: white;
            border-radius: 2px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.5);
            transform: translateX(-50%);
            transition: left 0.5s ease;
        }
        
        .funding-labels {
            display: flex;
            justify-content: space-between;
            font-size: 10px;
            color: var(--text-muted);
            margin-top: 8px;
        }
        
        /* Liquidation Bars */
        .liq-bar-wrap {
            display: flex;
            align-items: center;
            gap: 12px;
            margin: 12px 0;
        }
        
        .liq-label {
            width: 50px;
            font-size: 11px;
            color: var(--text-muted);
            text-align: center;
        }
        
        .liq-bar {
            flex: 1;
            height: 28px;
            background: var(--bg-secondary);
            border-radius: 14px;
            overflow: hidden;
            display: flex;
        }
        
        .liq-shorts {
            background: linear-gradient(90deg, var(--bad), #dc2626);
            display: flex;
            align-items: center;
            justify-content: flex-end;
            padding-right: 10px;
            font-size: 11px;
            font-weight: 700;
            color: white;
        }
        
        .liq-longs {
            background: linear-gradient(90deg, #16a34a, var(--good));
            display: flex;
            align-items: center;
            padding-left: 10px;
            font-size: 11px;
            font-weight: 700;
            color: white;
        }
        
        /* Long/Short Ratio */
        .ls-ratio-bar {
            height: 16px;
            border-radius: 8px;
            overflow: hidden;
            display: flex;
            margin: 12px 0;
        }
        
        .ls-long {
            background: var(--good);
            transition: width 0.5s;
        }
        
        .ls-short {
            background: var(--bad);
            transition: width 0.5s;
        }
        
        .ls-labels {
            display: flex;
            justify-content: space-between;
            font-size: 12px;
            font-weight: 600;
        }
        
        /* Signal Box */
        .signal-box {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 12px;
            background: var(--bg-secondary);
            border-radius: 10px;
            margin-top: 12px;
        }
        
        .signal-icon {
            font-size: 24px;
        }
        
        .signal-text {
            flex: 1;
        }
        
        .signal-title {
            font-size: 13px;
            font-weight: 700;
        }
        
        .signal-desc {
            font-size: 11px;
            color: var(--text-muted);
        }
        
        /* Tab Content */
        .tab-content {
            display: none;
        }
        
        .tab-content.active {
            display: block;
        }
        
        /* Loading */
        .loading {
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 60px;
            color: var(--text-muted);
            font-size: 13px;
        }
        
        .spinner {
            width: 20px;
            height: 20px;
            border: 2px solid var(--border);
            border-top-color: var(--accent);
            border-radius: 50%;
            animation: spin 0.8s linear infinite;
            margin-right: 10px;
        }
        
        @keyframes spin { to { transform: rotate(360deg); } }
        
        /* Time Selector */
        .time-selector {
            display: flex;
            gap: 4px;
            background: var(--bg-secondary);
            padding: 3px;
            border-radius: 6px;
            border: 1px solid var(--border);
        }
        
        .time-btn {
            padding: 4px 10px;
            border-radius: 4px;
            font-size: 11px;
            font-weight: 600;
            color: var(--text-muted);
            cursor: pointer;
            border: none;
            background: none;
            transition: all 0.2s;
        }
        
        .time-btn:hover { color: var(--text); }
        .time-btn.active { background: var(--panel); color: var(--text); }
        
        /* Footer */
        footer {
            text-align: center;
            padding: 24px;
            color: var(--text-dim);
            font-size: 12px;
            border-top: 1px solid var(--border);
            margin-top: 32px;
        }
        
        footer a {
            color: var(--accent);
            text-decoration: none;
        }
        
        /* Error State */
        .error-state {
            text-align: center;
            padding: 40px;
            color: var(--text-muted);
        }
        
        .error-icon {
            font-size: 48px;
            margin-bottom: 16px;
        }
        
        .error-title {
            font-size: 16px;
            font-weight: 600;
            margin-bottom: 8px;
        }
        
        .error-desc {
            font-size: 13px;
            color: var(--text-dim);
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="nav">
        <div class="nav-content">
            <div class="logo">
                <div class="logo-icon">F</div>
                <div class="logo-text">FLOWTRACK</div>
            </div>
            
            <div class="nav-tabs">
                <button class="nav-tab active" data-tab="overview">Overview</button>
                <button class="nav-tab" data-tab="funding">Funding</button>
                <button class="nav-tab" data-tab="oi">Open Interest</button>
                <button class="nav-tab" data-tab="liquidations">Liquidations</button>
            </div>
            
            <div class="nav-status">
                <div class="status-pill">
                    <span class="status-dot"></span>
                    <span id="lastUpdate">Connecting...</span>
                </div>
                <button class="refresh-btn" onclick="refreshAllData()" id="refreshBtn">
                    ↻ Refresh
                </button>
            </div>
        </div>
    </nav>

    <div class="container">
        <!-- API Key Configuration -->
        <div class="api-banner" id="apiBanner">
            <div class="api-banner-icon">🔑</div>
            <div class="api-banner-text">
                <div class="api-banner-title">Coinalyze API Connected</div>
                <div class="api-banner-desc">Data source: Aggregated futures data from all major exchanges</div>
            </div>
            <div class="api-input-group">
                <input type="text" class="api-input" id="apiKeyInput" placeholder="Enter Coinalyze API Key" value="18b514e4-84aa-42a7-a567-bd6c18fd774d">
                <button class="api-btn" onclick="saveApiKey()">Update</button>
            </div>
        </div>

        <!-- Summary Row -->
        <div class="summary-row">
            <div class="summary-card">
                <div class="summary-label">Global Funding Rate</div>
                <div class="summary-value" id="summaryFunding">--</div>
                <div class="summary-change" id="summaryFundingSignal">Loading...</div>
            </div>
            <div class="summary-card">
                <div class="summary-label">Total Open Interest</div>
                <div class="summary-value" id="summaryOI">--</div>
                <div class="summary-change" id="summaryOIChange">--</div>
            </div>
            <div class="summary-card">
                <div class="summary-label">24h Liquidations</div>
                <div class="summary-value" id="summaryLiq">--</div>
                <div class="summary-change" id="summaryLiqRatio">--</div>
            </div>
            <div class="summary-card">
                <div class="summary-label">BTC OI Dominance</div>
                <div class="summary-value" id="summaryBTCDom">--</div>
                <div class="summary-change" id="summaryBTCDomChange">--</div>
            </div>
            <div class="summary-card">
                <div class="summary-label">Long/Short Ratio</div>
                <div class="summary-value" id="summaryLSRatio">--</div>
                <div class="summary-change" id="summaryLSSignal">--</div>
            </div>
        </div>

        <!-- OVERVIEW TAB -->
        <div class="tab-content active" id="tab-overview">
            <div class="grid-2">
                <!-- Aggregated Funding Rate Chart -->
                <div class="card">
                    <div class="card-header">
                        <div class="card-title">📊 Global Aggregated Funding Rate</div>
                        <div class="time-selector">
                            <button class="time-btn" onclick="updateFundingChart('7d')">7D</button>
                            <button class="time-btn active" onclick="updateFundingChart('30d')">30D</button>
                            <button class="time-btn" onclick="updateFundingChart('90d')">90D</button>
                            <button class="time-btn" onclick="updateFundingChart('1y')">1Y</button>
                        </div>
                    </div>
                    <div class="chart-container tall">
                        <canvas id="fundingChart"></canvas>
                    </div>
                    <div class="funding-meter">
                        <div class="funding-pointer" id="fundingPointer" style="left: 50%;"></div>
                    </div>
                    <div class="funding-labels">
                        <span>-0.1% (Shorts Pay)</span>
                        <span>0%</span>
                        <span>+0.1% (Longs Pay)</span>
                    </div>
                    <div class="signal-box" id="fundingSignal">
                        <div class="signal-icon">⚖️</div>
                        <div class="signal-text">
                            <div class="signal-title">Neutral Funding</div>
                            <div class="signal-desc">Market in equilibrium</div>
                        </div>
                    </div>
                </div>
                
                <!-- OI Dominance Stacked Chart -->
                <div class="card">
                    <div class="card-header">
                        <div class="card-title">📈 Open Interest Dominance</div>
                        <div class="time-selector">
                            <button class="time-btn" onclick="updateOIChart('7d')">7D</button>
                            <button class="time-btn active" onclick="updateOIChart('30d')">30D</button>
                            <button class="time-btn" onclick="updateOIChart('90d')">90D</button>
                            <button class="time-btn" onclick="updateOIChart('1y')">1Y</button>
                        </div>
                    </div>
                    <div class="chart-container tall">
                        <canvas id="oiDominanceChart"></canvas>
                    </div>
                    <div class="oi-dominance-bar" id="oiDominanceBar">
                        <div class="oi-segment btc" id="oiBTC" style="width: 46%;">BTC 46%</div>
                        <div class="oi-segment eth" id="oiETH" style="width: 26%;">ETH 26%</div>
                        <div class="oi-segment others" id="oiOthers" style="width: 28%;">Others 28%</div>
                    </div>
                    <div class="oi-legend">
                        <div class="oi-legend-item"><div class="oi-legend-dot" style="background: var(--accent);"></div>BTC <span id="btcOIValue">$30.0B</span></div>
                        <div class="oi-legend-item"><div class="oi-legend-dot" style="background: var(--cyan);"></div>ETH <span id="ethOIValue">$16.5B</span></div>
                        <div class="oi-legend-item"><div class="oi-legend-dot" style="background: var(--orange);"></div>Others <span id="othersOIValue">$18.2B</span></div>
                    </div>
                </div>
            </div>
            
            <div class="grid-3" style="margin-top: 20px;">
                <!-- 24h Liquidations -->
                <div class="card">
                    <div class="card-header">
                        <div class="card-title">⚡ 24h Liquidations</div>
                        <div class="card-badge" id="liqBadge">--</div>
                    </div>
                    <div class="card-value" id="totalLiq">--</div>
                    <div class="card-subtitle">Total liquidated in 24 hours</div>
                    <div class="liq-bar-wrap">
                        <div class="liq-label">Shorts</div>
                        <div class="liq-bar">
                            <div class="liq-shorts" id="liqShorts" style="width: 40%;">$45M</div>
                            <div class="liq-longs" id="liqLongs" style="width: 60%;">$67M</div>
                        </div>
                        <div class="liq-label">Longs</div>
                    </div>
                    <div class="signal-box" id="liqSignal">
                        <div class="signal-icon">📊</div>
                        <div class="signal-text">
                            <div class="signal-title">More Longs Liquidated</div>
                            <div class="signal-desc">Sellers in control</div>
                        </div>
                    </div>
                </div>
                
                <!-- Long/Short Ratio -->
                <div class="card">
                    <div class="card-header">
                        <div class="card-title">📊 Global Long/Short Ratio</div>
                        <div class="card-badge" id="lsBadge">--</div>
                    </div>
                    <div class="card-value" id="lsRatioValue">--</div>
                    <div class="card-subtitle">Top traders' account positioning</div>
                    <div class="ls-ratio-bar">
                        <div class="ls-long" id="lsLong" style="width: 52%;"></div>
                        <div class="ls-short" id="lsShort" style="width: 48%;"></div>
                    </div>
                    <div class="ls-labels">
                        <span style="color: var(--good);" id="lsLongPct">Longs: 52%</span>
                        <span style="color: var(--bad);" id="lsShortPct">Shorts: 48%</span>
                    </div>
                    <div class="signal-box" id="lsSignal">
                        <div class="signal-icon">⚖️</div>
                        <div class="signal-text">
                            <div class="signal-title">Slight Long Bias</div>
                            <div class="signal-desc">No extreme positioning</div>
                        </div>
                    </div>
                </div>
                
                <!-- Predicted Funding -->
                <div class="card">
                    <div class="card-header">
                        <div class="card-title">🔮 Predicted Funding Rate</div>
                        <div class="card-badge" id="predFundingBadge">--</div>
                    </div>
                    <div class="card-value" id="predFundingValue">--</div>
                    <div class="card-subtitle">Next funding period estimate</div>
                    <div class="signal-box" style="margin-top: 24px;" id="predFundingSignal">
                        <div class="signal-icon">📈</div>
                        <div class="signal-text">
                            <div class="signal-title">Funding Expected to...</div>
                            <div class="signal-desc">Based on current order book imbalance</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- FUNDING TAB -->
        <div class="tab-content" id="tab-funding">
            <div class="card" style="margin-bottom: 20px;">
                <div class="card-header">
                    <div class="card-title">📊 Aggregated Funding Rate History (All Coins)</div>
                    <div class="time-selector">
                        <button class="time-btn" onclick="updateFundingHistoryChart('1d')">1D</button>
                        <button class="time-btn" onclick="updateFundingHistoryChart('7d')">7D</button>
                        <button class="time-btn active" onclick="updateFundingHistoryChart('30d')">30D</button>
                        <button class="time-btn" onclick="updateFundingHistoryChart('90d')">90D</button>
                        <button class="time-btn" onclick="updateFundingHistoryChart('1y')">1Y</button>
                    </div>
                </div>
                <div class="chart-container tall">
                    <canvas id="fundingHistoryChart"></canvas>
                </div>
            </div>
            
            <div class="grid-2">
                <div class="card">
                    <div class="card-header">
                        <div class="card-title">BTC Funding Rate</div>
                    </div>
                    <div class="chart-container">
                        <canvas id="btcFundingChart"></canvas>
                    </div>
                </div>
                <div class="card">
                    <div class="card-header">
                        <div class="card-title">ETH Funding Rate</div>
                    </div>
                    <div class="chart-container">
                        <canvas id="ethFundingChart"></canvas>
                    </div>
                </div>
            </div>
        </div>

        <!-- OI TAB -->
        <div class="tab-content" id="tab-oi">
            <div class="card" style="margin-bottom: 20px;">
                <div class="card-header">
                    <div class="card-title">📈 Total Open Interest (All Exchanges)</div>
                    <div class="time-selector">
                        <button class="time-btn" onclick="updateOIHistoryChart('7d')">7D</button>
                        <button class="time-btn active" onclick="updateOIHistoryChart('30d')">30D</button>
                        <button class="time-btn" onclick="updateOIHistoryChart('90d')">90D</button>
                        <button class="time-btn" onclick="updateOIHistoryChart('1y')">1Y</button>
                        <button class="time-btn" onclick="updateOIHistoryChart('2y')">2Y</button>
                    </div>
                </div>
                <div class="chart-container tall">
                    <canvas id="oiHistoryChart"></canvas>
                </div>
            </div>
            
            <div class="grid-3">
                <div class="card">
                    <div class="card-header">
                        <div class="card-title">BTC Open Interest</div>
                        <div class="card-badge badge-neutral">PERP + FUTURES</div>
                    </div>
                    <div class="card-value" id="btcOITotal">--</div>
                    <div class="card-change" id="btcOIChange">--</div>
                </div>
                <div class="card">
                    <div class="card-header">
                        <div class="card-title">ETH Open Interest</div>
                        <div class="card-badge badge-neutral">PERP + FUTURES</div>
                    </div>
                    <div class="card-value" id="ethOITotal">--</div>
                    <div class="card-change" id="ethOIChange">--</div>
                </div>
                <div class="card">
                    <div class="card-header">
                        <div class="card-title">Altcoins Open Interest</div>
                        <div class="card-badge badge-neutral">ALL OTHERS</div>
                    </div>
                    <div class="card-value" id="altOITotal">--</div>
                    <div class="card-change" id="altOIChange">--</div>
                </div>
            </div>
        </div>

        <!-- LIQUIDATIONS TAB -->
        <div class="tab-content" id="tab-liquidations">
            <div class="card" style="margin-bottom: 20px;">
                <div class="card-header">
                    <div class="card-title">⚡ Liquidation History</div>
                    <div class="time-selector">
                        <button class="time-btn" onclick="updateLiqHistoryChart('1d')">1D</button>
                        <button class="time-btn active" onclick="updateLiqHistoryChart('7d')">7D</button>
                        <button class="time-btn" onclick="updateLiqHistoryChart('30d')">30D</button>
                        <button class="time-btn" onclick="updateLiqHistoryChart('90d')">90D</button>
                    </div>
                </div>
                <div class="chart-container tall">
                    <canvas id="liqHistoryChart"></canvas>
                </div>
            </div>
            
            <div class="grid-2">
                <div class="card">
                    <div class="card-header">
                        <div class="card-title">BTC Liquidations (24h)</div>
                    </div>
                    <div style="display: flex; gap: 20px; margin-top: 16px;">
                        <div style="flex: 1; text-align: center;">
                            <div style="font-size: 24px; font-weight: 800; color: var(--good);" id="btcLongLiq">--</div>
                            <div style="font-size: 12px; color: var(--text-muted);">Long Liquidations</div>
                        </div>
                        <div style="flex: 1; text-align: center;">
                            <div style="font-size: 24px; font-weight: 800; color: var(--bad);" id="btcShortLiq">--</div>
                            <div style="font-size: 12px; color: var(--text-muted);">Short Liquidations</div>
                        </div>
                    </div>
                </div>
                <div class="card">
                    <div class="card-header">
                        <div class="card-title">ETH Liquidations (24h)</div>
                    </div>
                    <div style="display: flex; gap: 20px; margin-top: 16px;">
                        <div style="flex: 1; text-align: center;">
                            <div style="font-size: 24px; font-weight: 800; color: var(--good);" id="ethLongLiq">--</div>
                            <div style="font-size: 12px; color: var(--text-muted);">Long Liquidations</div>
                        </div>
                        <div style="flex: 1; text-align: center;">
                            <div style="font-size: 24px; font-weight: 800; color: var(--bad);" id="ethShortLiq">--</div>
                            <div style="font-size: 12px; color: var(--text-muted);">Short Liquidations</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <footer>
        <p><strong>FLOWTRACK</strong> — Crypto Derivatives Intelligence</p>
        <p style="margin-top: 6px;">Data from <a href="https://coinalyze.net" target="_blank">Coinalyze</a> • Aggregated across all major exchanges</p>
        <p style="margin-top: 6px;">Not financial advice • For informational purposes only</p>
    </footer>

    <script>
        // ==================== CONFIGURATION ====================
        const COINALYZE_API = 'https://api.coinalyze.net/v1';
        let API_KEY = '18b514e4-84aa-42a7-a567-bd6c18fd774d';
        
        // Symbol mappings for aggregated data
        // Using .A suffix for aggregated data across all exchanges
        const SYMBOLS = {
            BTC_PERP: 'BTCUSD_PERP.A',
            ETH_PERP: 'ETHUSD_PERP.A',
            // Individual exchanges use suffixes like .0 (Binance), .2 (Bybit), etc.
        };
        
        // Chart instances
        let fundingChart = null;
        let oiDominanceChart = null;
        let fundingHistoryChart = null;
        let oiHistoryChart = null;
        let liqHistoryChart = null;
        let btcFundingChart = null;
        let ethFundingChart = null;
        
        // ==================== UTILITIES ====================
        function formatNumber(num, decimals = 2) {
            if (num === null || num === undefined || isNaN(num)) return '--';
            return new Intl.NumberFormat('en-US', { 
                minimumFractionDigits: decimals,
                maximumFractionDigits: decimals 
            }).format(num);
        }
        
        function formatCurrency(num) {
            if (num === null || num === undefined || isNaN(num)) return '--';
            if (num >= 1e12) return '$' + (num / 1e12).toFixed(2) + 'T';
            if (num >= 1e9) return '$' + (num / 1e9).toFixed(2) + 'B';
            if (num >= 1e6) return '$' + (num / 1e6).toFixed(2) + 'M';
            if (num >= 1e3) return '$' + (num / 1e3).toFixed(1) + 'K';
            return '$' + formatNumber(num, 2);
        }
        
        function formatPercent(num, showSign = true) {
            if (num === null || num === undefined || isNaN(num)) return '--';
            const sign = showSign && num > 0 ? '+' : '';
            return sign + formatNumber(num, 4) + '%';
        }
        
        function getTimestampRange(period) {
            const now = Math.floor(Date.now() / 1000);
            const periods = {
                '1d': 86400,
                '7d': 7 * 86400,
                '30d': 30 * 86400,
                '90d': 90 * 86400,
                '1y': 365 * 86400,
                '2y': 2 * 365 * 86400
            };
            return {
                from: now - (periods[period] || periods['30d']),
                to: now
            };
        }
        
        function getInterval(period) {
            const intervals = {
                '1d': '1hour',
                '7d': '4hour',
                '30d': 'daily',
                '90d': 'daily',
                '1y': 'daily',
                '2y': 'daily'
            };
            return intervals[period] || 'daily';
        }
        
        // ==================== API CALLS ====================
        async function coinalyzeAPI(endpoint, params = {}) {
            const url = new URL(COINALYZE_API + endpoint);
            url.searchParams.append('api_key', API_KEY);
            Object.entries(params).forEach(([key, value]) => {
                url.searchParams.append(key, value);
            });
            
            try {
                const response = await fetch(url.toString());
                if (!response.ok) {
                    if (response.status === 401) throw new Error('Invalid API key');
                    if (response.status === 429) throw new Error('Rate limited');
                    throw new Error(`API error: ${response.status}`);
                }
                return await response.json();
            } catch (error) {
                console.error(`Coinalyze API error (${endpoint}):`, error);
                throw error;
            }
        }
        
        // Get current funding rate
        async function getCurrentFundingRate(symbols) {
            return await coinalyzeAPI('/funding-rate', { symbols: symbols.join(',') });
        }
        
        // Get funding rate history
        async function getFundingRateHistory(symbols, period = '30d') {
            const { from, to } = getTimestampRange(period);
            return await coinalyzeAPI('/funding-rate-history', {
                symbols: symbols.join(','),
                interval: getInterval(period),
                from,
                to
            });
        }
        
        // Get current open interest
        async function getCurrentOI(symbols) {
            return await coinalyzeAPI('/open-interest', { 
                symbols: symbols.join(','),
                convert_to_usd: 'true'
            });
        }
        
        // Get OI history
        async function getOIHistory(symbols, period = '30d') {
            const { from, to } = getTimestampRange(period);
            return await coinalyzeAPI('/open-interest-history', {
                symbols: symbols.join(','),
                interval: getInterval(period),
                from,
                to,
                convert_to_usd: 'true'
            });
        }
        
        // Get predicted funding rate
        async function getPredictedFundingRate(symbols) {
            return await coinalyzeAPI('/predicted-funding-rate', { symbols: symbols.join(',') });
        }
        
        // Get liquidation history
        async function getLiquidationHistory(symbols, period = '7d') {
            const { from, to } = getTimestampRange(period);
            return await coinalyzeAPI('/liquidation-history', {
                symbols: symbols.join(','),
                interval: getInterval(period),
                from,
                to,
                convert_to_usd: 'true'
            });
        }
        
        // Get long/short ratio history
        async function getLongShortRatioHistory(symbols, period = '30d') {
            const { from, to } = getTimestampRange(period);
            return await coinalyzeAPI('/long-short-ratio-history', {
                symbols: symbols.join(','),
                interval: getInterval(period),
                from,
                to
            });
        }
        
        // ==================== CHART CREATION ====================
        function createFundingChart(data) {
            const ctx = document.getElementById('fundingChart').getContext('2d');
            
            if (fundingChart) fundingChart.destroy();
            
            fundingChart = new Chart(ctx, {
                type: 'bar',
                data: {
                    labels: data.map(d => new Date(d.t * 1000)),
                    datasets: [{
                        data: data.map(d => d.c * 100), // Convert to percentage
                        backgroundColor: data.map(d => d.c >= 0 ? 'rgba(16, 185, 129, 0.8)' : 'rgba(239, 68, 68, 0.8)'),
                        borderRadius: 2
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: { display: false },
                        tooltip: {
                            callbacks: {
                                label: (ctx) => `Funding: ${formatPercent(ctx.raw, true)}`
                            }
                        }
                    },
                    scales: {
                        x: {
                            type: 'time',
                            grid: { color: 'rgba(255,255,255,0.05)' },
                            ticks: { color: '#6b7280', maxTicksLimit: 8 }
                        },
                        y: {
                            grid: { color: 'rgba(255,255,255,0.05)' },
                            ticks: { 
                                color: '#6b7280',
                                callback: (v) => v.toFixed(3) + '%'
                            }
                        }
                    }
                }
            });
        }
        
        function createOIDominanceChart(btcData, ethData, othersData) {
            const ctx = document.getElementById('oiDominanceChart').getContext('2d');
            
            if (oiDominanceChart) oiDominanceChart.destroy();
            
            // Align data by timestamps
            const labels = btcData.map(d => new Date(d.t * 1000));
            
            oiDominanceChart = new Chart(ctx, {
                type: 'line',
                data: {
                    labels,
                    datasets: [
                        {
                            label: 'BTC',
                            data: btcData.map(d => d.c / 1e9),
                            backgroundColor: 'rgba(59, 130, 246, 0.3)',
                            borderColor: '#3b82f6',
                            fill: true,
                            tension: 0.3
                        },
                        {
                            label: 'ETH',
                            data: ethData.map(d => d.c / 1e9),
                            backgroundColor: 'rgba(6, 182, 212, 0.3)',
                            borderColor: '#06b6d4',
                            fill: true,
                            tension: 0.3
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: { 
                            display: true,
                            position: 'top',
                            labels: { color: '#9ca3af', usePointStyle: true }
                        },
                        tooltip: {
                            callbacks: {
                                label: (ctx) => `${ctx.dataset.label}: $${ctx.raw.toFixed(2)}B`
                            }
                        }
                    },
                    scales: {
                        x: {
                            type: 'time',
                            grid: { color: 'rgba(255,255,255,0.05)' },
                            ticks: { color: '#6b7280', maxTicksLimit: 8 }
                        },
                        y: {
                            stacked: false,
                            grid: { color: 'rgba(255,255,255,0.05)' },
                            ticks: { 
                                color: '#6b7280',
                                callback: (v) => '$' + v + 'B'
                            }
                        }
                    }
                }
            });
        }
        
        function createLiquidationChart(data) {
            const ctx = document.getElementById('liqHistoryChart').getContext('2d');
            
            if (liqHistoryChart) liqHistoryChart.destroy();
            
            const labels = data.map(d => new Date(d.t * 1000));
            
            liqHistoryChart = new Chart(ctx, {
                type: 'bar',
                data: {
                    labels,
                    datasets: [
                        {
                            label: 'Long Liquidations',
                            data: data.map(d => d.l / 1e6),
                            backgroundColor: 'rgba(16, 185, 129, 0.8)'
                        },
                        {
                            label: 'Short Liquidations',
                            data: data.map(d => -d.s / 1e6), // Negative for stacking below
                            backgroundColor: 'rgba(239, 68, 68, 0.8)'
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: { 
                            display: true,
                            position: 'top',
                            labels: { color: '#9ca3af', usePointStyle: true }
                        },
                        tooltip: {
                            callbacks: {
                                label: (ctx) => `${ctx.dataset.label}: $${Math.abs(ctx.raw).toFixed(2)}M`
                            }
                        }
                    },
                    scales: {
                        x: {
                            type: 'time',
                            stacked: true,
                            grid: { color: 'rgba(255,255,255,0.05)' },
                            ticks: { color: '#6b7280', maxTicksLimit: 10 }
                        },
                        y: {
                            stacked: true,
                            grid: { color: 'rgba(255,255,255,0.05)' },
                            ticks: { 
                                color: '#6b7280',
                                callback: (v) => '$' + Math.abs(v).toFixed(0) + 'M'
                            }
                        }
                    }
                }
            });
        }
        
        // ==================== UI UPDATE FUNCTIONS ====================
        async function updateOverviewData() {
            try {
                // Get current funding rates for BTC and ETH
                const fundingData = await getCurrentFundingRate([SYMBOLS.BTC_PERP, SYMBOLS.ETH_PERP]);
                
                // Calculate average funding
                const avgFunding = fundingData.reduce((sum, d) => sum + d.value, 0) / fundingData.length;
                
                // Update summary
                document.getElementById('summaryFunding').textContent = formatPercent(avgFunding * 100, true);
                document.getElementById('summaryFunding').style.color = avgFunding > 0.0001 ? 'var(--bad)' : avgFunding < -0.0001 ? 'var(--good)' : 'var(--text)';
                
                // Update funding signal
                const fundingSignalEl = document.getElementById('summaryFundingSignal');
                if (avgFunding > 0.0003) {
                    fundingSignalEl.textContent = '🔥 Longs Crowded';
                    fundingSignalEl.style.color = 'var(--bad)';
                } else if (avgFunding < -0.0003) {
                    fundingSignalEl.textContent = '❄️ Shorts Crowded';
                    fundingSignalEl.style.color = 'var(--good)';
                } else {
                    fundingSignalEl.textContent = '⚖️ Neutral';
                    fundingSignalEl.style.color = 'var(--text-muted)';
                }
                
                // Update funding meter
                const pointerPos = Math.min(100, Math.max(0, (avgFunding * 100 + 0.1) / 0.2 * 100));
                document.getElementById('fundingPointer').style.left = pointerPos + '%';
                
                // Update funding signal box
                const signalBox = document.getElementById('fundingSignal');
                if (avgFunding > 0.0005) {
                    signalBox.innerHTML = `
                        <div class="signal-icon">🔥</div>
                        <div class="signal-text">
                            <div class="signal-title" style="color: var(--bad);">Extreme Greed</div>
                            <div class="signal-desc">Longs paying heavy premium - squeeze risk elevated</div>
                        </div>
                    `;
                } else if (avgFunding < -0.0005) {
                    signalBox.innerHTML = `
                        <div class="signal-icon">❄️</div>
                        <div class="signal-text">
                            <div class="signal-title" style="color: var(--good);">Extreme Fear</div>
                            <div class="signal-desc">Shorts paying premium - short squeeze setup</div>
                        </div>
                    `;
                } else {
                    signalBox.innerHTML = `
                        <div class="signal-icon">⚖️</div>
                        <div class="signal-text">
                            <div class="signal-title">Neutral Funding</div>
                            <div class="signal-desc">Market in equilibrium - no extreme positioning</div>
                        </div>
                    `;
                }
                
            } catch (error) {
                console.error('Error updating overview:', error);
            }
        }
        
        async function updateOIData() {
            try {
                const oiData = await getCurrentOI([SYMBOLS.BTC_PERP, SYMBOLS.ETH_PERP]);
                
                let btcOI = 0, ethOI = 0;
                oiData.forEach(d => {
                    if (d.symbol.includes('BTC')) btcOI = d.value;
                    if (d.symbol.includes('ETH')) ethOI = d.value;
                });
                
                // For demo, estimate total OI (real implementation would aggregate more symbols)
                const totalOI = (btcOI + ethOI) * 1.8; // Rough estimate including alts
                const othersOI = totalOI - btcOI - ethOI;
                
                // Update summary
                document.getElementById('summaryOI').textContent = formatCurrency(totalOI);
                document.getElementById('summaryBTCDom').textContent = formatNumber(btcOI / totalOI * 100, 1) + '%';
                
                // Update OI breakdown
                document.getElementById('btcOIValue').textContent = formatCurrency(btcOI);
                document.getElementById('ethOIValue').textContent = formatCurrency(ethOI);
                document.getElementById('othersOIValue').textContent = formatCurrency(othersOI);
                
                // Update dominance bar
                const btcPct = btcOI / totalOI * 100;
                const ethPct = ethOI / totalOI * 100;
                const othersPct = 100 - btcPct - ethPct;
                
                document.getElementById('oiBTC').style.width = btcPct + '%';
                document.getElementById('oiBTC').textContent = `BTC ${formatNumber(btcPct, 0)}%`;
                document.getElementById('oiETH').style.width = ethPct + '%';
                document.getElementById('oiETH').textContent = `ETH ${formatNumber(ethPct, 0)}%`;
                document.getElementById('oiOthers').style.width = othersPct + '%';
                document.getElementById('oiOthers').textContent = `Others ${formatNumber(othersPct, 0)}%`;
                
                // Update individual OI cards
                document.getElementById('btcOITotal').textContent = formatCurrency(btcOI);
                document.getElementById('ethOITotal').textContent = formatCurrency(ethOI);
                document.getElementById('altOITotal').textContent = formatCurrency(othersOI);
                
            } catch (error) {
                console.error('Error updating OI:', error);
            }
        }
        
        async function updateFundingChart(period = '30d') {
            try {
                // Update active button
                document.querySelectorAll('#tab-overview .time-btn').forEach(btn => {
                    btn.classList.toggle('active', btn.textContent.toLowerCase() === period);
                });
                
                const history = await getFundingRateHistory([SYMBOLS.BTC_PERP], period);
                if (history && history[0] && history[0].history) {
                    createFundingChart(history[0].history);
                }
            } catch (error) {
                console.error('Error updating funding chart:', error);
            }
        }
        
        async function updateOIChart(period = '30d') {
            try {
                const [btcHistory, ethHistory] = await Promise.all([
                    getOIHistory([SYMBOLS.BTC_PERP], period),
                    getOIHistory([SYMBOLS.ETH_PERP], period)
                ]);
                
                if (btcHistory && btcHistory[0] && ethHistory && ethHistory[0]) {
                    createOIDominanceChart(
                        btcHistory[0].history,
                        ethHistory[0].history,
                        []
                    );
                }
            } catch (error) {
                console.error('Error updating OI chart:', error);
            }
        }
        
        async function updateLiquidationData() {
            try {
                const liqHistory = await getLiquidationHistory([SYMBOLS.BTC_PERP, SYMBOLS.ETH_PERP], '1d');
                
                let totalLongs = 0, totalShorts = 0;
                let btcLongs = 0, btcShorts = 0;
                let ethLongs = 0, ethShorts = 0;
                
                liqHistory.forEach(item => {
                    if (item.history) {
                        item.history.forEach(h => {
                            if (item.symbol.includes('BTC')) {
                                btcLongs += h.l || 0;
                                btcShorts += h.s || 0;
                            } else if (item.symbol.includes('ETH')) {
                                ethLongs += h.l || 0;
                                ethShorts += h.s || 0;
                            }
                            totalLongs += h.l || 0;
                            totalShorts += h.s || 0;
                        });
                    }
                });
                
                const totalLiq = totalLongs + totalShorts;
                const longPct = totalLongs / totalLiq * 100;
                
                // Update summary
                document.getElementById('summaryLiq').textContent = formatCurrency(totalLiq);
                document.getElementById('summaryLiqRatio').textContent = longPct > 55 ? '📉 Long Pain' : longPct < 45 ? '📈 Short Squeeze' : '⚖️ Balanced';
                document.getElementById('summaryLiqRatio').style.color = longPct > 55 ? 'var(--bad)' : longPct < 45 ? 'var(--good)' : 'var(--text-muted)';
                
                // Update liquidation card
                document.getElementById('totalLiq').textContent = formatCurrency(totalLiq);
                document.getElementById('liqShorts').textContent = formatCurrency(totalShorts);
                document.getElementById('liqShorts').style.width = (100 - longPct) + '%';
                document.getElementById('liqLongs').textContent = formatCurrency(totalLongs);
                document.getElementById('liqLongs').style.width = longPct + '%';
                
                // Update badge
                const liqBadge = document.getElementById('liqBadge');
                if (longPct > 60) {
                    liqBadge.textContent = 'LONG PAIN';
                    liqBadge.className = 'card-badge badge-bad';
                } else if (longPct < 40) {
                    liqBadge.textContent = 'SHORT SQUEEZE';
                    liqBadge.className = 'card-badge badge-good';
                } else {
                    liqBadge.textContent = 'BALANCED';
                    liqBadge.className = 'card-badge badge-neutral';
                }
                
                // Update individual liquidation displays
                document.getElementById('btcLongLiq').textContent = formatCurrency(btcLongs);
                document.getElementById('btcShortLiq').textContent = formatCurrency(btcShorts);
                document.getElementById('ethLongLiq').textContent = formatCurrency(ethLongs);
                document.getElementById('ethShortLiq').textContent = formatCurrency(ethShorts);
                
                // Update signal
                const liqSignal = document.getElementById('liqSignal');
                if (longPct > 60) {
                    liqSignal.innerHTML = `
                        <div class="signal-icon">📉</div>
                        <div class="signal-text">
                            <div class="signal-title" style="color: var(--bad);">Long Liquidation Cascade</div>
                            <div class="signal-desc">Sellers in control - overleveraged longs getting flushed</div>
                        </div>
                    `;
                } else if (longPct < 40) {
                    liqSignal.innerHTML = `
                        <div class="signal-icon">📈</div>
                        <div class="signal-text">
                            <div class="signal-title" style="color: var(--good);">Short Liquidation Cascade</div>
                            <div class="signal-desc">Buyers in control - shorts getting squeezed</div>
                        </div>
                    `;
                } else {
                    liqSignal.innerHTML = `
                        <div class="signal-icon">⚖️</div>
                        <div class="signal-text">
                            <div class="signal-title">Balanced Liquidations</div>
                            <div class="signal-desc">No dominant side - choppy conditions</div>
                        </div>
                    `;
                }
                
            } catch (error) {
                console.error('Error updating liquidations:', error);
            }
        }
        
        async function updateLongShortRatio() {
            try {
                const lsHistory = await getLongShortRatioHistory([SYMBOLS.BTC_PERP], '1d');
                
                if (lsHistory && lsHistory[0] && lsHistory[0].history && lsHistory[0].history.length > 0) {
                    const latest = lsHistory[0].history[lsHistory[0].history.length - 1];
                    const ratio = latest.r;
                    const longPct = latest.l * 100;
                    const shortPct = latest.s * 100;
                    
                    // Update summary
                    document.getElementById('summaryLSRatio').textContent = formatNumber(ratio, 2);
                    const lsSignalSummary = document.getElementById('summaryLSSignal');
                    if (ratio > 1.2) {
                        lsSignalSummary.textContent = '📈 Long Bias';
                        lsSignalSummary.style.color = 'var(--good)';
                    } else if (ratio < 0.8) {
                        lsSignalSummary.textContent = '📉 Short Bias';
                        lsSignalSummary.style.color = 'var(--bad)';
                    } else {
                        lsSignalSummary.textContent = '⚖️ Balanced';
                        lsSignalSummary.style.color = 'var(--text-muted)';
                    }
                    
                    // Update card
                    document.getElementById('lsRatioValue').textContent = formatNumber(ratio, 2);
                    document.getElementById('lsLong').style.width = longPct + '%';
                    document.getElementById('lsShort').style.width = shortPct + '%';
                    document.getElementById('lsLongPct').textContent = `Longs: ${formatNumber(longPct, 1)}%`;
                    document.getElementById('lsShortPct').textContent = `Shorts: ${formatNumber(shortPct, 1)}%`;
                    
                    // Update badge and signal
                    const lsBadge = document.getElementById('lsBadge');
                    const lsSignal = document.getElementById('lsSignal');
                    
                    if (ratio > 1.5) {
                        lsBadge.textContent = 'LONG CROWDED';
                        lsBadge.className = 'card-badge badge-bad';
                        lsSignal.innerHTML = `
                            <div class="signal-icon">⚠️</div>
                            <div class="signal-text">
                                <div class="signal-title" style="color: var(--bad);">Extreme Long Bias</div>
                                <div class="signal-desc">Contrarian signal - potential long squeeze ahead</div>
                            </div>
                        `;
                    } else if (ratio < 0.67) {
                        lsBadge.textContent = 'SHORT CROWDED';
                        lsBadge.className = 'card-badge badge-good';
                        lsSignal.innerHTML = `
                            <div class="signal-icon">⚠️</div>
                            <div class="signal-text">
                                <div class="signal-title" style="color: var(--good);">Extreme Short Bias</div>
                                <div class="signal-desc">Contrarian signal - potential short squeeze ahead</div>
                            </div>
                        `;
                    } else {
                        lsBadge.textContent = 'BALANCED';
                        lsBadge.className = 'card-badge badge-neutral';
                        lsSignal.innerHTML = `
                            <div class="signal-icon">⚖️</div>
                            <div class="signal-text">
                                <div class="signal-title">Balanced Positioning</div>
                                <div class="signal-desc">No extreme bias detected</div>
                            </div>
                        `;
                    }
                }
            } catch (error) {
                console.error('Error updating L/S ratio:', error);
            }
        }
        
        async function updatePredictedFunding() {
            try {
                const predFunding = await getPredictedFundingRate([SYMBOLS.BTC_PERP, SYMBOLS.ETH_PERP]);
                
                const avgPred = predFunding.reduce((sum, d) => sum + d.value, 0) / predFunding.length;
                
                document.getElementById('predFundingValue').textContent = formatPercent(avgPred * 100, true);
                document.getElementById('predFundingValue').style.color = avgPred > 0.0001 ? 'var(--bad)' : avgPred < -0.0001 ? 'var(--good)' : 'var(--text)';
                
                const badge = document.getElementById('predFundingBadge');
                const signal = document.getElementById('predFundingSignal');
                
                if (avgPred > 0.0003) {
                    badge.textContent = 'LONGS PAY';
                    badge.className = 'card-badge badge-bad';
                    signal.innerHTML = `
                        <div class="signal-icon">📈</div>
                        <div class="signal-text">
                            <div class="signal-title">Funding Rising</div>
                            <div class="signal-desc">Order book shows more long demand</div>
                        </div>
                    `;
                } else if (avgPred < -0.0003) {
                    badge.textContent = 'SHORTS PAY';
                    badge.className = 'card-badge badge-good';
                    signal.innerHTML = `
                        <div class="signal-icon">📉</div>
                        <div class="signal-text">
                            <div class="signal-title">Funding Falling</div>
                            <div class="signal-desc">Order book shows more short demand</div>
                        </div>
                    `;
                } else {
                    badge.textContent = 'NEUTRAL';
                    badge.className = 'card-badge badge-neutral';
                    signal.innerHTML = `
                        <div class="signal-icon">➡️</div>
                        <div class="signal-text">
                            <div class="signal-title">Funding Stable</div>
                            <div class="signal-desc">Balanced order book pressure</div>
                        </div>
                    `;
                }
            } catch (error) {
                console.error('Error updating predicted funding:', error);
            }
        }
        
        async function updateLiqHistoryChart(period = '7d') {
            try {
                const liqHistory = await getLiquidationHistory([SYMBOLS.BTC_PERP], period);
                if (liqHistory && liqHistory[0] && liqHistory[0].history) {
                    createLiquidationChart(liqHistory[0].history);
                }
            } catch (error) {
                console.error('Error updating liq chart:', error);
            }
        }
        
        // ==================== TAB NAVIGATION ====================
        document.querySelectorAll('.nav-tab[data-tab]').forEach(tab => {
            tab.addEventListener('click', () => {
                document.querySelectorAll('.nav-tab[data-tab]').forEach(t => t.classList.remove('active'));
                document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('active'));
                tab.classList.add('active');
                document.getElementById('tab-' + tab.dataset.tab).classList.add('active');
            });
        });
        
        // ==================== API KEY MANAGEMENT ====================
        function saveApiKey() {
            API_KEY = document.getElementById('apiKeyInput').value.trim();
            localStorage.setItem('coinalyze_api_key', API_KEY);
            refreshAllData();
        }
        
        function loadApiKey() {
            const saved = localStorage.getItem('coinalyze_api_key');
            if (saved) {
                API_KEY = saved;
                document.getElementById('apiKeyInput').value = saved;
            }
        }
        
        // ==================== MAIN REFRESH ====================
        async function refreshAllData() {
            const btn = document.getElementById('refreshBtn');
            btn.disabled = true;
            btn.textContent = '↻ Loading...';
            
            try {
                await Promise.all([
                    updateOverviewData(),
                    updateOIData(),
                    updateFundingChart('30d'),
                    updateOIChart('30d'),
                    updateLiquidationData(),
                    updateLongShortRatio(),
                    updatePredictedFunding()
                ]);
                
                document.getElementById('lastUpdate').textContent = new Date().toLocaleTimeString();
                
            } catch (error) {
                console.error('Error refreshing data:', error);
                document.getElementById('lastUpdate').textContent = 'Error - Check API Key';
            } finally {
                btn.disabled = false;
                btn.textContent = '↻ Refresh';
            }
        }
        
        // ==================== INITIALIZE ====================
        document.addEventListener('DOMContentLoaded', () => {
            loadApiKey();
            refreshAllData();
            
            // Auto refresh every 60 seconds
            setInterval(refreshAllData, 60000);
        });
    </script>
</body>
</html>
